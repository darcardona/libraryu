<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans     
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context 
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- tells Spring to scan the code for injectable beans annotated with @Component, 
		@Controller, @Repository, and @Service as well as supporting the @Autowired 
		and @Inject annotations under the package (and all its subpackages) specified -->
	<context:component-scan
		base-package="com.dar.training.libraryu.services,
						com.dar.training.libraryu.repository, 
						com.dar.training.libraryu.model" />

	<!-- configure Spring to automatically enable the Spring type conversion 
		system and field formatting, as well as support of JSR-349 Bean Validation -->
	<mvc:annotation-driven />

	<!-- Rest Template -->
	<bean id="restTemplate" class="org.springframework.web.client.RestTemplate">
		
	</bean>

</beans>